import React from "react";
import ReservationForm from "../components/ReservationForm";

const Reserve = () => {
  return (
    <div className="h-full w-full bg-gray-100 flex justify-center items-center flex-col">
      <ReservationForm />
    </div>
  );
};

export default Reserve;
